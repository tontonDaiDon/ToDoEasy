<%= form_with(model: shopping_list) do |form| %>
  <% if shopping_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shopping_list.errors.count, "error") %> prohibited this shopping_list from being saved:</h2>

      <ul>
        <% shopping_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :budget, style: "display: block" %>
    <%= form.number_field :budget %>
  </div>

  <div class="field">
    <%= form.label :shopped_on, "リスト作成日" %>
    <%= form.date_select :shopped_on %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
