<!DOCTYPE html>
<html>
  <head>
    <title>ToDoEasy2</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag('favicon.ico') %>
  </head>

  <body class="bg-light text-dark">

  <% if user_signed_in? %>
  <!-- ナビゲーション -->
  <header class="app-header d-flex align-items-center justify-content-between px-3 py-2">
    <!-- 左：トップに戻る -->
    <div class="nav-left">
      <%= link_to 'トップに戻る', root_path, class: "btn btn-top" %>
    </div>

    <!-- 中央：あいさつ -->
    <div class="header-user text-center flex-grow-1">
      こんにちは、<%= (current_user&.nickname.presence || current_user&.email) %> さん
    </div>

    <!-- 右：ログアウト -->
    <div class="nav-right">
      <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "logout-btn btn-logout" %>
    </div>

    <!-- ハンバーガーボタン（スマホ用） -->
    <button class="menu-toggle" aria-label="メニューを開閉">☰</button>
  
    <!-- スマホ用メニュー -->
    <nav class="menu">
      <ul>
        <li><%= link_to 'トップに戻る', root_path, class: "btn btn-top" %></li>
        <li><%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "logout-btn btn-logout" %></li>
      </ul>
    </nav>
  </header>
<% end %>

    <!-- フラッシュメッセージ -->
    <div class="container">
      <% if flash[:notice] %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= flash[:notice] %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= flash[:alert] %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <!-- メインコンテンツ -->
    <main class="container mb-5">
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="app-footer bg-white border-top text-center text-muted py-3">
      <small>&copy; 2025 ToDoEasy. All rights reserved.</small>
    </footer>
  </body>
</html>
